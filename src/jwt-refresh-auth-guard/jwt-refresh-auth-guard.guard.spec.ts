import { JwtRefreshAuthGuardGuard } from './jwt-refresh-auth-guard.guard';

describe('JwtRefreshAuthGuardGuard', () => {
  it('should be defined', () => {
    expect(new JwtRefreshAuthGuardGuard()).toBeDefined();
  });
});
